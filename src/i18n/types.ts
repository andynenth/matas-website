// TypeScript types for translations
// Moved from main translations.ts

export type Language = 'th' | 'en';

// Re-export types from index for backwards compatibility
export type { TranslationData, TranslationLang } from './index.js';